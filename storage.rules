rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Dev-mode: matches the wide-open Firestore rules in firestore.rules.
      allow read, write: if true;
    }
  }
}
